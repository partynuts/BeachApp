import { StyleSheet } from 'react-native';
import colors from "../../colors";

export const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    fontWeight: "700",
    color: colors.black
  }
});
