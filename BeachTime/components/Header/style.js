import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    flexGrow: 1,
    alignItems: "center",
    alignContent: "flex-end",
  },
  greeting: {
    paddingRight: 5,
  }
});

